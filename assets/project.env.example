# Copy to assets/project.env and fill in your keys (project.env is gitignored).
# Rebuild the app after editing (hot reload does not reload env).

# Chat: OpenRouter is tried first; Groq is used if OpenRouter fails.
OPENROUTER_API_KEY=sk-or-v1-...
GROQ_API_KEY=gsk_...

# Voice note transcription uses GROQ_API_KEY only (Whisper).

# Optional custom NLP backend for chat (if set, tried before OpenRouter/Groq):
# NLP_API_URL=
# NLP_API_KEY=

# Contact Us (EmailJS — https://dashboard.emailjs.com)
# Recommendations are saved to Firestore collection "recommendations" (no email).
# REQUIRED for mobile APK: EmailJS → Account → Security → enable
# "Allow EmailJS API for non-browser applications" (otherwise sends fail with 403).
# Also set EMAILJS_PRIVATE_KEY below. Messages still save to Firestore contact_messages
# when EmailJS fails (deploy firestore.rules after updating).
# EMAILJS_SERVICE_ID=service_akm5fyg
# EMAILJS_TEMPLATE_ID=template_ujtn37d
# EMAILJS_PUBLIC_KEY=your_public_key
# EMAILJS_PRIVATE_KEY=your_private_key
# CONTACT_SUPPORT_EMAIL=you@example.com
